{
  "name": "@cycleplatform/pipeline-trigger-action",
  "version": "0.5.0",
  "description": "Trigger a pipeline on Cycle as a Github Action",
  "main": "index.ts",
  "scripts": {
    "build": "tsc && npm ci --only=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Petrichor Holdings, Inc.",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "1.11.1",
    "@cycleplatform/api-client-typescript": "0.3.7"
  },
  "devDependencies": {
    "@types/node": "22.13.0",
    "ts-node": "10.9.2",
    "typescript": "5.7.3"
  }
}
